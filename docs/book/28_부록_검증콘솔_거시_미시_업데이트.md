# 28. 부록: 검증콘솔(거시/미시) 업데이트 노트

최종 업데이트: `2026-03-08`

## 0) 목적
이 부록은 책 00~27의 이론/예측을 현재 웹 검증 콘솔 구현과 정합되게 연결하기 위한 운영 명세다.

### 0.0 기준 데이터셋 자료 출처
| 항목 | 내용 |
|---|---|
| dataset 버전/생성시각 | `frozen-20260308`, `2026-03-08T16:02:02Z` |
| 거시 출처 | GraceDB, GCN, HEASARC |
| 미시 출처 | HEPData, PDG, NuFIT |
| 참조 파일 경로 | `data/frozen/current/manifest.json`, `data/frozen/current/audit_manifest.json`, `docs/sources/CONFIRMED_EVENT_ENDPOINTS.md`, `docs/sources/OPEN_DATASETS_API_LIST.md` |

### 0.1 공식 접근 경로(운영 고정)
- 공식 도메인: `https://salt.numverse.org`
- 평가 경로: `/evaluation` (기준 데이터셋 기준 판정)
- 증거 시각화 경로: `/evidence` (채널별 비교 그래프/결과 확인)
- 감사/재현 경로: `/audit`

운영 정책:
- Evaluation은 버전 고정 입력(`data/frozen/current`)만 사용한다.
- Evidence는 기준 데이터셋 결과의 시각화와 비교 해석을 담당하며 판정 원본은 Evaluation/Audit 산출물이다.
- 거시 문맥의 기준 이론 표기는 `ΛCDM`, 미시 문맥은 `SM`으로 고정한다.

- 1단계: 거시(우주론) 검증 규격 고정
- 2단계: 미시(입자물리) 검증 규격 초안 고정

---

## 1) 거시(우주론) 업데이트: 웹 반영 완료 항목

### 1.1 표준이론 범위 고정
- 현재 웹 콘솔의 `Standard`는 **표준우주론(ΛCDM)**을 의미한다.
- 미시(입자물리) Standard Model(SM) 검증은 별도 트랙으로 분리한다.

### 1.2 데이터 비교 규격 (운영식)
거시 이벤트 단위 비교는 아래 3중 구조를 사용한다.

- `actual_value`: 실측값
- `standard_fit`: 표준우주론(ΛCDM) 예측값
- `salt_fit`: SALT 예측값

오차/판정은 아래 규칙으로 고정한다.

\[
\texttt{standard\_error}=actual\_value-standard\_fit
\]
\[
\texttt{salt\_error}=actual\_value-salt\_fit
\]
\[
\texttt{winner}=\arg\min\left(\left|standard\_error\right|,\left|salt\_error\right|\right)
\]

집계 지표:
- `MAE`, `RMSE`, `avg_delta_fit`

통계 기준:
- `alpha=0.05`
- `FDR(BH) q=0.10`
- 효과크기 기준 병행

### 1.3 관측 채널 정합
거시 예측식/판정 채널은 24장 13.4를 기준으로 유지한다.
- Shapiro 지연
- 중력 적색편이
- 강중력 렌즈 지연
- GNSS/GPS
- GW-EM 도착시각

참조:
- 24장 `13.4 검증 프로토콜 표`
- 26장 `예측 6.1 (LIV)`

### 1.4 웹 페이지 대응
- 상위: `Evaluation` / `Evidence` / `Audit`
- `Evaluation`: frozen 기반 실측 vs ΛCDM/SM vs SALT 비교 및 최종 판정
- `Evidence`: 시각화(채널별 시계열/비교 그래프)와 요약 해석 제공
- `Audit`: 데이터/식/판정규칙 버전과 재현 명령 추적

---

## 2) 미시(입자물리) 업데이트: 초안 고정

### 2.1 현재 상태 진단
- 책에는 미시 채널(뮤온 g-2, 중성미자, 충돌기) **검증 방향**은 제시되어 있다.
- 미시는 보셀 해상도 한계 때문에 **관측량별 완성 예측식의 완전잠금**을 목표로 두지 않는다.
- 대신 관측량/비교식 형태/파라미터 범위/통계판정 규칙을 고정하는 **운영잠금(operational lock)** 으로 진행한다.
- 따라서 미시는 `검증대기` 트랙으로 분리 운영한다.

### 2.2 이미 보유한 미시 관련 수식/채널
- 고에너지 LIV 분산식(26장):
\[
\omega^2 = k^2 + \eta\left(k^4/M^2\right)
\]
- 미시 후보 채널(17장):
  - 뮤온 g-2
  - 중성미자
  - 충돌기

### 2.3 미시 검증 데이터 계약(v0)
미시 데이터는 아래 최소 스키마를 사용한다.

- `observable_id` (예: `muon_g_minus_2`, `nu_osc_theta23`)
- `dataset_id` (예: 실험/런 구분)
- `measured_value`
- `measurement_uncertainty`
- `quality_flag`
- `source_url`
- `observed_at_utc`, `ingested_at_utc`

예측 엔진 산출 필드(입력 payload 직접 주입 금지):
- `sm_pred` (SM predictor engine output)
- `salt_pred` (SALT predictor engine output)
- `sm_residual = measured_value - sm_pred`
- `salt_residual = measured_value - salt_pred`
- `winner = argmin(|sm_residual|, |salt_residual|)`
- `engine_version`, `formula_version`, `prediction_lock_sha256`

### 2.4 미시 반증 규칙(v0)
- 같은 관측량에서 `|salt_residual| < |sm_residual|` 우세 비율을 채널별로 집계한다.
- 채널별 `MAE/RMSE`를 계산하고 다중비교 보정(FDR) 후 판정한다.
- 특정 채널에서 SALT 우세가 반복 재현되지 않으면 해당 채널 가설을 보류한다.

### 2.5 미시 다음 작업(운영잠금 필요)
아래 3개를 잠그면 웹 미시 검증을 시작할 수 있다.

1. 관측량/비교식 형태/파라미터 범위(뮤온 g-2, 중성미자 진동, 충돌기 잔차)
2. 기준 SM 예측 소스(HEPData/PDG/Fermilab/CERN Open Data 등)
3. 채널별 반증 임계값(통계량/유의수준/효과크기)
4. 책 본문(16/17/18/20/24/26) 엔진 분리 원칙 문구와 완전 동기화

---

## 3) 선언
- 본 부록의 거시 항목(1장)은 웹 운영 규격으로 즉시 적용한다.
- 본 부록의 미시 항목(2장)은 검증대기 명세이며, 운영잠금 전에는 결과 해석을 확정하지 않는다.
